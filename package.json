{
	"name": "nbt",
	"displayName": "NBT",
	"description": "NBT Editor for VSCode",
	"version": "0.0.1",
	"publisher": "Misode",
	"enableProposedApi": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/misode/vscode-nbt.git"
	},
	"engines": {
		"vscode": "^1.42.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.helloWorld",
		"onWebviewEditor:nbtEditor.nbt"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "extension.helloWorld",
				"title": "Hello World"
			}
		],
		"webviewEditors": [
			{
				"viewType": "nbtEditor.nbt",
				"displayName": "NBT Data",
				"selector": [
					{
						"filenamePattern": "*.nbt"
					}
				]
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/node": "^12.11.7",
		"@types/vscode": "^1.42.0",
		"glob": "^7.1.6",
		"typescript": "^3.7.5"
	}
}
